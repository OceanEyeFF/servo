---
title: "Distribution Safety & Version Traceability"
artifact_type: "milestone"
generated_from: "init-milestone-skill"
updated: "2026-05-19"
owner: "servo-kernel"
---
# Milestone Artifact

## milestone_id

```yaml
milestone_id: "MS-20260519-002"
```

## title

```yaml
title: "Distribution Safety & Version Traceability"
```

## purpose

```yaml
purpose: "Establish operator-facing distribution safety before further productization by adding a Harness payload version marker contract, uninstall/remove guidance, and managed-files ownership boundaries."
```

## status

```yaml
status: "completed"
```

## worktrack_list

```yaml
worktrack_list:
  - worktrack_id: "WT-20260519-harness-version-marker-contract"
    expected_status: "done"
  - worktrack_id: "WT-20260519-uninstall-remove-runbook"
    expected_status: "done"
  - worktrack_id: "WT-20260519-managed-files-ownership-sync"
    expected_status: "done"
```

## completion_signals

```yaml
completion_signals:
  - "Harness distribution payload version is represented by a stable source marker adjacent to the canonical harness-skill entry."
  - "Operator-facing docs explain how installed version facts should be interpreted without treating deploy targets as source truth."
  - "Uninstall/remove guidance explains the safe prune --all boundary for agents, claude, and bundle."
  - "Managed-files ownership docs clearly distinguish installer-managed skill payloads from .servo/ runtime control state and user-owned repository files."
  - "Deploy and usage-help entrypoints route operators to the version, uninstall, and ownership guidance without duplicating policy."
```

## acceptance_criteria

```yaml
acceptance_criteria:
  - "A version marker file exists at the approved canonical source location and its semantics are documented."
  - "Uninstall/remove docs state what is deleted, what is retained, and how bundle mode behaves."
  - "Managed-files ownership guidance identifies installer-owned paths, repo runtime state, deploy targets, and non-goals."
  - "Relevant deploy, usage-help, and governance references are updated only where they own entrypoint routing or policy."
  - "Required governance checks for path and docs changes pass or retained warnings are recorded."
  - "No package version, npm publish, release tag, dist-tag, repo rename, or dedicated distribution repository change is performed."
```

## completion_threshold_pct

```yaml
completion_threshold_pct: 100
```

## progress_counter

```yaml
progress_counter:
  total: 3
  completed: 3
  blocked: 0
  deferred: 0
```

## environment_probe

```yaml
environment_probe:
  checks:
    - name: "distribution-safety-docs"
      description: "Verify version marker, uninstall/remove, ownership docs, and routing entrypoints when the milestone is active."
      probe_command: "targeted docs review plus path/governance checks"
```

## aggregated_evidence

```yaml
aggregated_evidence: []
```

## release_version_consideration

```yaml
release_version_consideration: "This milestone may define a Harness payload version marker, but it does not authorize package version changes, npm publish, release tags, dist-tag mutation, or release channel changes."
```

## developer_decision_boundary

```yaml
developer_decision_boundary:
  - "Activation of this planned milestone remains programmer-owned."
  - "Repo rename and dedicated distribution repository strategy are explicitly out of scope and remain discussion topics."
  - "Any package version, release, publish, tag, or dist-tag change requires separate explicit approval."
```

## depends_on_milestones

```yaml
depends_on_milestones:
  - "MS-20260519-001"
```

## priority

```yaml
priority: 10
```

## activation_rules

```yaml
activation_rules: "manual programmer activation required; registering this milestone does not start WorktrackScope Init"
```

## milestone_kind

```yaml
milestone_kind: "goal-driven"
```

## updated

```yaml
updated: "2026-05-19T12:00:00+08:00"
```

## milestone_acceptance_state

```yaml
milestone_acceptance_state:
  worktrack_list_finished: true
  milestone_gate_ready: true
  programmer_final_acceptance_required: true
  status_reason: "Accepted by programmer 2026-05-19. All 3 worktracks closed and gate-passed."
```

## planning_boundary

```yaml
planning_boundary:
  baseline_ref: "develop-aw@fcd40980d58688d7ea11e66117a555f106b5db96"
  non_goals:
    - "do not change servo-installer executable behavior except for source marker packaging if required by the version marker contract"
    - "do not alter release channel, package version, npm dist-tags, or GitHub Releases"
    - "do not initialize worktracks until the milestone is explicitly activated"
    - "do not include repo rename or dedicated distribution repository decisions"
```
