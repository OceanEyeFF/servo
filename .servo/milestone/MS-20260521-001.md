---
title: "Milestone: .aw Runtime Seamless Upgrade"
artifact_type: milestone
generated_from: init-milestone-skill
updated: 2026-05-22T10:11:07+08:00
owner: servo-kernel
---

# Milestone: .aw Runtime Seamless Upgrade

## milestone_id

MS-20260521-001

## title

.aw Runtime Seamless Upgrade

## purpose

为仍在使用 `.aw/` runtime state 的目标仓库提供一条低摩擦升级路径：显式迁移 `.aw/` 到 `.servo/`，保留或备份用户状态，然后通过 `servo-installer` 重新覆盖安装 Skills，使 marker、target dirs、payload 指纹与新命名收敛。

## status

active

## milestone_kind

goal-driven

## priority

1

## depends_on_milestones

- MS-20260520-002

## worktrack_list

- WT-20260521-aw-upgrade-contract (active)
- WT-20260521-aw-to-servo-runtime-migrator (planned)
- WT-20260521-skill-marker-reinstall-upgrade-flow (planned)
- WT-20260521-aw-upgrade-docs-and-smoke (planned)

## worktrack_sequence

1. WT-1: `.aw` upgrade contract（检测、冲突、备份、dry-run、恢复、marker 刷新边界）
2. WT-2: `.aw` to `.servo` runtime migrator（显式入口、`--dry-run`、`--yes`、备份、幂等、`/tmp` 烟测）
3. WT-3: skill marker reinstall upgrade flow（迁移后通过 installer 覆盖安装刷新 Skills marker、legacy cleanup 与 payload fingerprint）
4. WT-4: upgrade docs and smoke（runbook、usage docs、目标仓库矩阵与治理验证）

## completion_signals

- 只含 `.aw/` 的旧目标仓库可以通过显式命令升级到 `.servo/`，runtime state 保持等价。
- 已存在 `.servo/` 的目标仓库默认阻断迁移，并给出可执行恢复建议，不静默覆盖。
- `.aw/` 默认不删除；迁移保留原目录或创建备份，除非用户显式确认清理。
- 覆盖安装后，Skills marker、payload fingerprint、legacy target cleanup 与当前 installer 规则一致。
- 升级烟测在 `/tmp` 临时目标仓库中覆盖 `.aw` only、`.servo` exists、两者同时存在、foreign marker、损坏 marker、dry-run、备份恢复场景。
- operator-facing docs 清楚说明旧 `.aw` 用户的升级路径。

## acceptance_criteria

- 提供显式迁移入口，不把 `.aw -> .servo` 作为普通初始化时的隐式写操作。
- 迁移入口支持 dry-run，输出将执行的复制、备份、阻断或覆盖安装建议。
- 迁移入口支持幂等执行；重复运行不会破坏已迁移的 `.servo/` state。
- `.servo/` 冲突默认阻断；只有用户明确确认的恢复路径可以继续。
- 覆盖安装流程复用现有 `aw.marker` / legacy cleanup / payload fingerprint 机制，并通过测试验证。
- `/tmp` 目标仓库烟测覆盖主要迁移场景，不在源码树下生成测试 runtime state。
- 相关 installer 测试、folder/path/governance 检查通过。
- docs/runbook 与 usage entrypoint 同步更新。

## completion_threshold_pct

100

## planning_boundary

- do not delete user-owned `.aw/` contents by default
- do not silently overwrite existing `.servo/`
- do not modify package version, npm dist-tag, release tag, publish state, or release channel policy
- do not treat `.agents/` or `.claude/` deploy targets as source truth
- do not modify `.autoworkflow/` or `.spec-workflow/` directories
- use `/tmp` target repositories for migration smoke tests

## progress_counter

- total: 4
- completed: 0
- blocked: 0
- deferred: 0

## release_version_consideration

No release or version mutation is owned by this milestone. Release readiness may be reported as evidence only.

## developer_decision_boundary

- Activation remains programmer-owned after MS-20260520-002 is accepted.
- Any default deletion of `.aw/` after migration requires explicit programmer approval.
- Any change to release channel, package version, npm publish state, or dist-tag is out of scope.

## updated

2026-05-22T10:11:07+08:00
