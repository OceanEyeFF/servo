---
title: "Milestone: Repo Rename to servo"
artifact_type: milestone
generated_from: harness-skill
updated: 2026-05-22T01:03:06+08:00
owner: servo-kernel
---

# Milestone: Repo Rename to servo

## milestone_id

MS-20260520-002

## title

Repo Rename to servo

## purpose

将仓库、npm 包、内部前缀从 `aw-` / `autoworkflow` 统一重命名为 `servo`，使项目身份与「AI 编码的闭环控制系统」内核一致。外部面先改（GitHub repo + npm package），然后内部全量迁移（skills / docs / templates / scripts），最后运行时目录和治理检查收尾。

## status

completed

## milestone_kind

goal-driven

## priority

0

## depends_on_milestones

- MS-20260520-001

## worktrack_list

- WT-20260520-servo-external-rename (done)
- WT-20260520-servo-internal-migration (done)
- WT-20260520-servo-runtime-governance-closeout (done)
- WT-20260520-servo-npm-release-prep (done, appended)

## worktrack_sequence

1. WT-1: 外部面重命名（GitHub repo rename、npm 发布 `servo-installer` 首版、README/入口文档更新、旧 `servo-installer` 标记 deprecated）
2. WT-2: 内部全量迁移（`aw-` → `servo-` 全局替换：skills、docs、templates、scripts、deploy）
3. WT-3: 运行时 & 治理收尾（`.servo/` → `.servo/` + control-state 路径更新 + governance check 适配 + 改名白名单/黑名单验收）

## completion_signals

- GitHub repo 名从 `servo` 变为 `servo`
- npm 上 `servo-installer@0.5.3` 可安装，latest 指向 `0.5.3`
- 源码中 `servo-installer`、`servo-harness`、`servo-kernel`、`servo`、`.servo/` 引用清零（历史 changelog 中的版本号除外）
- `servo-installer`、`servo-harness` 新名在全量源码中可搜到
- `npx servo-installer --help` 成功
- governance_semantic_check + path_governance_check + folder_logic_check 通过
- 所有 134 现有测试通过

## acceptance_criteria

### 白名单清零（必须改到无残留）

| 搜索项 | 排除范围 | 验收线 |
|--------|---------|--------|
| `servo-installer` | `CHANGELOG*`, `.git/` | = 0 matches |
| `servo-harness` | — | = 0 matches |
| `servo-kernel` | — | = 0 matches |
| `servo` | `.git/` | = 0 matches |
| `.servo/` (源码引用) | `.autoworkflow/`, `.git/` | = 0 matches |

### 新名存在检查

| 搜索项 | 验收线 |
|--------|--------|
| `servo-installer` | count > 0 |
| `servo-harness` | count > 0 |

### 功能验证

| 检查项 | 验收线 |
|--------|--------|
| `npx servo-installer --help` | 成功执行 |
| `governance_semantic_check.py --json` | passed |
| `path_governance_check.py` | passed |
| `folder_logic_check.py` | passed |
| 现有测试套件 | 134 tests pass |

## completion_threshold_pct

100

## planning_boundary

- do not modify `.autoworkflow/` directory or its contents
- do not modify `.spec-workflow/` directory
- do not modify `.agents/` or `.claude/` deploy targets
- do not rewrite git history
- do not change package version, release tag, dist-tag, or release channel policy during rename
- `servo-installer` npm package must remain available as deprecated, not unpublished
- changelog/historical references to old name are preserved

## progress_counter

- total: 4
- completed: 4
- blocked: 0
- deferred: 0

## aggregated_evidence

- npm registry: `servo-installer@0.5.3` published; `latest` dist-tag points to `0.5.3`.
- published gitHead: `f8076d1235e7d44a04a3372ac64c9f1c98afb8ad`.
- tarball: `https://registry.npmjs.org/servo-installer/-/servo-installer-0.5.3.tgz`.
- integrity: `sha512-Yx+XcaOm8+NQv/soqhg6LyzO6nAUhvRE7KDXMPq9DQzl+mC+QV6FtJ3fbZ7RpEpoKSfOC3NvypOEgbhJwF9e1g==`.
- `npx --yes servo-installer@latest --version`: `servo-installer 0.5.3`.
- `npx --yes servo-installer@latest --help`: passed.
- `governance_semantic_check.py`: passed.
- `closeout_acceptance_gate.py --json`: passed.

## acceptance

- accepted_by: programmer
- accepted_at: 2026-05-22T01:03:06+08:00
- verdict: accepted
- note: all 4 worktracks closed or release-verified; npm publish and registry npx smoke evidence confirmed.

## updated

2026-05-22T01:03:06+08:00
