---
title: "Context Route Slimming"
artifact_type: "milestone"
generated_from: "harness-skill"
updated: "2026-05-13"
owner: "servo-kernel"
---
# Milestone Artifact

## milestone_id

```yaml
milestone_id: "MS-20260513-001"
```

## title

```yaml
title: "Context Route Slimming"
```

## purpose

```yaml
purpose: "Reduce the default agent reading path so boot context flows through AGENTS.md -> INDEX.md -> one task-local entry, with expansion only on explicit governance, layering, Harness artifact, dispatch, gate, or closeout triggers."
```

## status

```yaml
status: "completed"
```

## worktrack_list

```yaml
worktrack_list:
  - worktrack_id: "WT-20260513-m1-route-contract-slimming"
    expected_status: "done"
```

## completion_signals

```yaml
completion_signals:
  - "AGENTS.md no longer contains a fixed long Read First preload list"
  - "INDEX.md is the task routing handoff after AGENTS.md"
  - "repo-onboarding and review/verify governance describe the lightweight boot route"
  - "governance_semantic_check.py fails if AGENTS.md regresses to the fixed Read First preload shape"
```

## acceptance_criteria

```yaml
acceptance_criteria:
  - "AGENTS.md defines Default Boot as AGENTS.md + INDEX.md + one task-local entry"
  - "INDEX.md distinguishes default boot from on-demand expansion"
  - "review-verify-handbook.md lists route contract changes and their verification commands"
  - "targeted governance semantic tests pass"
```

## completion_threshold_pct

```yaml
completion_threshold_pct: 100
```

## progress_counter

```yaml
progress_counter:
  total: 1
  completed: 1
  blocked: 0
  deferred: 0
```

## environment_probe

```yaml
environment_probe:
  checks:
    - name: "route-governance"
      description: "Run path_governance_check.py and governance_semantic_check.py after route contract edits."
    - name: "targeted-pytest"
      description: "Run targeted governance_semantic_check regression tests."
```

## aggregated_evidence

```yaml
aggregated_evidence:
  - "git: develop-main@acb6b402c91a74af00aa678582c48a826609bacb"
  - "PYTHONDONTWRITEBYTECODE=1 python3 -m pytest toolchain/scripts/test/test_governance_semantic_check.py -q: passed"
  - "PYTHONDONTWRITEBYTECODE=1 python3 toolchain/scripts/test/governance_semantic_check.py --json: passed"
  - "PYTHONDONTWRITEBYTECODE=1 python3 toolchain/scripts/test/path_governance_check.py: passed"
  - "PYTHONDONTWRITEBYTECODE=1 python3 toolchain/scripts/test/folder_logic_check.py: passed"
  - "git diff --check: passed"
```

## release_version_consideration

```yaml
release_version_consideration: "No release action implied; this milestone only changes route governance and documentation contracts."
```

## developer_decision_boundary

```yaml
developer_decision_boundary:
  - "This execution cycle may complete only MS-20260513-001; later milestones remain planned backlog."
  - "Dangerous operations remain approval-gated."
```

## depends_on_milestones

```yaml
depends_on_milestones: []
```

## priority

```yaml
priority: 1
```

## activation_rules

```yaml
activation_rules: "Programmer explicitly approved adding the four recommended milestones and completing one milestone in this execution cycle on 2026-05-13."
```

## created_by

```yaml
created_by: "programmer"
```

## milestone_kind

```yaml
milestone_kind: "goal-driven"
```

## updated

```yaml
updated: "2026-05-13T00:00:00+08:00"
```
