---
title: "Human-First TUI Contract"
artifact_type: "milestone"
generated_from: "init-milestone-skill"
updated: "2026-05-19"
owner: "servo-kernel"
---
# Milestone Artifact

## milestone_id

```yaml
milestone_id: "MS-20260519-003"
```

## title

```yaml
title: "Human-First TUI Contract"
```

## purpose

```yaml
purpose: "Define the product contract for making servo-installer's TUI the human operator path while preserving CLI as the stable AI, CI, and script interface."
```

## status

```yaml
status: "completed"
```

## worktrack_list

```yaml
worktrack_list:
  - worktrack_id: "WT-20260519-tui-human-cli-contract"
    expected_status: "planned"
  - worktrack_id: "WT-20260519-tui-bundle-default-contract"
    expected_status: "planned"
  - worktrack_id: "WT-20260519-tui-usage-entrypoint-sync"
    expected_status: "planned"
```

## completion_signals

```yaml
completion_signals:
  - "Distribution entrypoint contract states that TUI is the recommended human flow and CLI remains the stable machine interface."
  - "TUI default backend selection is specified as bundle while CLI continues to require explicit backend selection."
  - "TUI screen model includes fixed status information for version, source, target repo, selected backend, current step, and verification result."
  - "Color semantics are defined as secondary status cues and never the only carrier of state."
  - "Human-facing quickstart and usage-help docs route new operators through the TUI flow without changing CLI contracts."
```

## acceptance_criteria

```yaml
acceptance_criteria:
  - "TUI/CLI responsibility split is documented in the owning deploy contract."
  - "Bundle default applies only to TUI unless an explicit CLI behavior change is separately approved."
  - "Guided flow contract covers diagnose, preview paths, confirm, install/update, verify, and final summary."
  - "Docs distinguish human, AI, CI, and script usage paths."
  - "Required path and governance checks pass or retained warnings are recorded."
  - "No TUI implementation, package version change, publish action, repo rename, or distribution repository decision is performed."
```

## completion_threshold_pct

```yaml
completion_threshold_pct: 100
```

## progress_counter

```yaml
progress_counter:
  total: 3
  completed: 3
  blocked: 0
  deferred: 0
```

## environment_probe

```yaml
environment_probe:
  checks:
    - name: "tui-contract-docs"
      description: "Verify the TUI/CLI contract, bundle default boundary, and human-facing usage routing when active."
      probe_command: "targeted deploy/usage docs review plus path/governance checks"
```

## aggregated_evidence

```yaml
aggregated_evidence: []
```

## release_version_consideration

```yaml
release_version_consideration: "This milestone defines TUI product contract only and does not authorize package version changes, npm publish, release tags, dist-tag mutation, or release channel changes."
```

## developer_decision_boundary

```yaml
developer_decision_boundary:
  - "Activation of this planned milestone remains programmer-owned."
  - "Changing CLI defaults requires separate explicit approval."
  - "Repo rename and dedicated distribution repository strategy are explicitly out of scope and remain discussion topics."
```

## depends_on_milestones

```yaml
depends_on_milestones:
  - "MS-20260519-002"
```

## priority

```yaml
priority: 11
```

## activation_rules

```yaml
activation_rules: "manual programmer activation required after MS-20260519-002 acceptance"
```

## milestone_kind

```yaml
milestone_kind: "goal-driven"
```

## updated

```yaml
updated: "2026-05-19"
```

## milestone_acceptance_state

```yaml
milestone_acceptance_state:
  worktrack_list_finished: false
  milestone_gate_ready: false
  programmer_final_acceptance_required: true
  status_reason: "Accepted by programmer 2026-05-19. All 3 worktracks closed and gate-passed."
```

## planning_boundary

```yaml
planning_boundary:
  baseline_ref: "develop-aw@fcd40980d58688d7ea11e66117a555f106b5db96"
  non_goals:
    - "define contract only; do not implement TUI layout or behavior in this milestone"
    - "preserve existing CLI deploy semantics"
    - "do not include repo rename or dedicated distribution repository decisions"
```
