---
title: "TUI Full-Flow Implementation"
artifact_type: "milestone"
generated_from: "init-milestone-skill"
updated: "2026-05-19"
owner: "servo-kernel"
---
# Milestone Artifact

## milestone_id

```yaml
milestone_id: "MS-20260519-004"
```

## title

```yaml
title: "TUI Full-Flow Implementation"
```

## purpose

```yaml
purpose: "Implement and verify the human-first servo-installer TUI flow defined by MS-20260519-003, including fixed layout, color semantics, bundle default, guided install/update, verification, and recovery surfaces."
```

## status

```yaml
status: "completed"
```

## worktrack_list

```yaml
worktrack_list:
  - worktrack_id: "WT-20260519-tui-layout-color-implementation"
    expected_status: "done"
  - worktrack_id: "WT-20260519-tui-guided-bundle-flow"
    expected_status: "done"
  - worktrack_id: "WT-20260519-tui-recovery-summary-surface"
    expected_status: "done"
  - worktrack_id: "WT-20260519-tui-pty-and-package-smoke"
    expected_status: "done"
```

## completion_signals

```yaml
completion_signals:
  - "TUI renders a stable human-readable status area and does not scroll critical context out of view during normal flow."
  - "TUI defaults to bundle for human installation while preserving explicit backend override options."
  - "TUI guided install/update flow maps every mutating operation to existing CLI verbs and preserves deploy semantics."
  - "Partial failure and recovery hints are visible in TUI and remain consistent with CLI output."
  - "PTY and package smoke coverage exercise the TUI flow sufficiently for release readiness decisions."
```

## acceptance_criteria

```yaml
acceptance_criteria:
  - "Implementation follows the Human-First TUI Contract from MS-20260519-003."
  - "Existing CLI tests continue to pass."
  - "TUI PTY tests cover default bundle path, single-backend override, cancellation, verification failure, and success summary where feasible."
  - "Package/local smoke demonstrates the TUI guard and at least one guided non-destructive or isolated target flow."
  - "Operator-facing docs are caught up with verified behavior."
  - "No package publish, release tag, dist-tag mutation, repo rename, or dedicated distribution repository decision is performed."
```

## completion_threshold_pct

```yaml
completion_threshold_pct: 100
```

## progress_counter

```yaml
progress_counter:
  total: 4
  completed: 4
  blocked: 0
  deferred: 0
```

## environment_probe

```yaml
environment_probe:
  checks:
    - name: "tui-implementation-smoke"
      description: "Verify TUI layout, guided bundle flow, CLI compatibility, and PTY/package smoke evidence when active."
      probe_command: "targeted npm tests, TUI PTY tests, and package/local smoke"
```

## aggregated_evidence

```yaml
aggregated_evidence: []
```

## release_version_consideration

```yaml
release_version_consideration: "This milestone may prepare release-ready TUI implementation evidence, but it does not authorize npm publish, release tags, dist-tag mutation, or release channel changes."
```

## developer_decision_boundary

```yaml
developer_decision_boundary:
  - "Activation of this planned milestone remains programmer-owned."
  - "Any release, publish, package version, tag, or dist-tag change requires separate explicit approval."
  - "Repo rename and dedicated distribution repository strategy are explicitly out of scope and remain discussion topics."
```

## depends_on_milestones

```yaml
depends_on_milestones:
  - "MS-20260519-003"
```

## priority

```yaml
priority: 12
```

## activation_rules

```yaml
activation_rules: "manual programmer activation required after MS-20260519-003 acceptance"
```

## milestone_kind

```yaml
milestone_kind: "goal-driven"
```

## updated

```yaml
updated: "2026-05-19"
```

## milestone_acceptance_state

```yaml
milestone_acceptance_state:
  worktrack_list_finished: true
  milestone_gate_ready: true
  programmer_final_acceptance_required: false
  status_reason: "Accepted by programmer 2026-05-20. All 4 worktracks + 7 refinement commits. 134 tests pass, guided flow verified end-to-end."
```

## planning_boundary

```yaml
planning_boundary:
  baseline_ref: "develop-aw@fcd40980d58688d7ea11e66117a555f106b5db96"
  non_goals:
    - "implement only the agreed TUI flow; do not redefine source trust or release channel policy"
    - "do not change CLI machine-readable output except where explicitly required by the shared contract"
    - "do not include repo rename or dedicated distribution repository decisions"
```
