{
  "name": "servo-installer",
  "version": "0.5.1-rc.1",
  "private": true,
  "description": "Local package scaffold for the AW Harness installer wrapper.",
  "bin": {
    "servo-installer": "bin/servo-installer.js"
  },
  "files": [
    "path_safety_policy.json",
    "bin/servo-installer.js"
  ],
  "scripts": {
    "smoke": "node bin/servo-installer.js --help",
    "test": "node --test test_servo_installer.js"
  }
}
